version: 1
direction: right
nodes:
  client: { label: Client }
  server: { label: Server }
edges:
  - from: client
    to: server
    labels:
      - { text: "request", at: start }
      - { text: "TLS 1.3", at: middle, style: { color: blue, size: 12 } }
      - { text: "response", at: end }
