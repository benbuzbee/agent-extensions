# Ignore real local secrets, but KEEP the committed .dev.vars.example template
# (copied to .dev.vars for local dev/tests — see README "Running tests").
.dev.vars
.dev.vars.*
!.dev.vars.example
.env*
dist/
node_modules/
.wrangler/

# Generated by `wrangler types` (npm run cf-typegen). Regenerate after wrangler.toml changes.
worker-configuration.d.ts

# Fresh template dropped by vendor.sh on re-vendor for manual merge; not your config.
wrangler.toml.upstream
